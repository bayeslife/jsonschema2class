/* {{{description}}} */
{{#if enum}}
public enum  {{title}}  {
  {{#each enum}}
    [EnumMember(Value="{{this}}")]
    {{this}},

  {{/each}}
{{else}}
public class  {{title}}  {

    {{#each properties}}

      /* {{this.description}} */
      public {{csharpPropertyType this}} {{@key}}   { get; set;}


    {{/each}}
  {{/if}}
}
