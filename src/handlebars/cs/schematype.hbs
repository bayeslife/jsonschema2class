/* {{{description}}} */
{{#if enum}}
public enum  {{title}}  {
  {{#each enum}}
    {{this}},
  {{/each}}
{{else}}
public class  {{title}}  {

    {{#each properties}}

      /* {{this.description}} */
      public {{csharpPropertyType this}} {{@key}}   { get; set;}


    {{/each}}
  {{/if}}
}
