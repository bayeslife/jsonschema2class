{{#if enum}}
public enum {{title}}  {
  {{#each enum}}
    {{this}}("{{this}}"),
  {{/each}};

  private String value;

  private {{title}}(String value) {
       this.value = value;
   }

 @Override
 public String toString() {
     return this.value;
 }
 }
{{else}}
public class  {{title}}  {

    {{#each properties}}
     {{#if pattern}}
     @Pattern(regexp = "{{{escape pattern}}}")
     {{/if}}
     private {{{javaPropertyType this}}} {{@key}};
    {{/each}}

    {{#each properties}}
      /* {{this.description}} */
      @XmlElement(required = {{required this ..}})
      @JsonProperty(required = {{required this ..}})
      public {{{javaPropertyType this}}} get{{methodCase @key}}(){
        return {{@key}};
      }

      public void set{{methodCase @key}}( {{{javaPropertyType this}}} t{{@key}} ) {
        {{@key}} = t{{@key}};
      }

    {{/each}}
  }
  {{/if}}
